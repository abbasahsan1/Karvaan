name: karvaan
description: "A vehicle management application."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  # Database & Storage
  mongo_dart: ^0.10.5
  shared_preferences: ^2.2.2
  
  # State Management
  provider: ^6.0.5  # Ensure this line is present and correct
  
  # UI & Formatting
  intl: ^0.20.2
  cupertino_icons: ^1.0.6
  flutter_svg: ^2.0.9
  google_fonts: ^6.1.0
  flutter_map: ^8.2.2
  latlong2: ^0.9.0
  geolocator: ^14.0.2
  
  # Functionality
  http: ^1.1.0
  path_provider: ^2.1.1
  url_launcher: ^6.2.1
  
  # Data Visualization
  fl_chart: ^1.1.0

  # UI Components
  loading_animation_widget: ^1.2.0+4

  # Add this line for password hashing
  encrypt: ^5.0.1
  crypto: ^3.0.3
  argon2: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  
  # Note: Assets commented out until directories are created
  # assets:
  #   - assets/images/
  #   - assets/icons/
  
  # Note: Using Google Fonts package instead of local fonts
  # fonts:
  #   - family: Poppins
  #     fonts:
  #       - asset: assets/fonts/Poppins-Regular.ttf
  #       - asset: assets/fonts/Poppins-Medium.ttf
  #         weight: 500
  #       - asset: assets/fonts/Poppins-Bold.ttf
  #         weight: 700
